// Root-level build.gradle (project build script)

buildscript {
    repositories {
        google()
        mavenCentral()
    }
}

plugins {
    // No Android plugins here
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
